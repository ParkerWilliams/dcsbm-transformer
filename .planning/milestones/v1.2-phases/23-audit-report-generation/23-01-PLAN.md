---
phase: 23-audit-report-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/reporting/audit_registry.py
  - src/reporting/vendor/katex.min.js
  - src/reporting/vendor/katex.min.css
autonomous: true
requirements: [REPT-01, REPT-02]

must_haves:
  truths:
    - "Registry contains exactly 28 entries, one per requirement ID (GRAPH-01..05, SVD-01..06, AUROC-01..04, STAT-01..06, SFTX-01..03, NULL-01..04)"
    - "Each entry has all required fields: req_id, category, title, latex, code_location, verdict, fix_description"
    - "Verdicts match audit findings: 24 correct, 4 fixed (GRAPH-04, SVD-05, STAT-05, NULL-02)"
    - "KaTeX JS and CSS files exist in vendor directory and are functional"
  artifacts:
    - path: "src/reporting/audit_registry.py"
      provides: "AUDIT_ENTRIES list of 28 dicts with all formula-to-code mappings"
      contains: "AUDIT_ENTRIES"
    - path: "src/reporting/vendor/katex.min.js"
      provides: "KaTeX JavaScript for LaTeX rendering"
    - path: "src/reporting/vendor/katex.min.css"
      provides: "KaTeX CSS with base64-embedded fonts"
  key_links:
    - from: "src/reporting/audit_registry.py"
      to: "tests/audit/*"
      via: "code_location references match actual source file:line locations"
      pattern: "src/.*\\.py:\\d+"
---

<objective>
Create the audit data registry (28 formula-to-code entries with verdicts) and vendor KaTeX assets for self-contained LaTeX rendering.

Purpose: This plan builds the data layer and rendering dependency for the audit report. The registry is the single source of truth for all formula-to-code mappings, verdicts, and fix descriptions from Phases 18-22. KaTeX assets enable offline LaTeX rendering in the final HTML.

Output: `src/reporting/audit_registry.py` with 28 entries, `src/reporting/vendor/` with KaTeX JS/CSS.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-audit-report-generation/23-CONTEXT.md

# Audit findings from all phase summaries — source of truth for verdicts
@.planning/phases/18-graph-walk-foundations/18-01-SUMMARY.md
@.planning/phases/18-graph-walk-foundations/18-02-SUMMARY.md
@.planning/phases/19-svd-metric-extraction/19-01-SUMMARY.md
@.planning/phases/19-svd-metric-extraction/19-02-SUMMARY.md
@.planning/phases/19-svd-metric-extraction/19-03-SUMMARY.md
@.planning/phases/20-auroc-predictive-horizon/20-01-SUMMARY.md
@.planning/phases/20-auroc-predictive-horizon/20-02-SUMMARY.md
@.planning/phases/21-statistical-controls/21-01-SUMMARY.md
@.planning/phases/21-statistical-controls/21-02-SUMMARY.md
@.planning/phases/22-softmax-bound-null-model/22-01-SUMMARY.md
@.planning/phases/22-softmax-bound-null-model/22-02-SUMMARY.md

# Existing LaTeX formulas for reference (NOT reused — fresh registry per user decision)
@src/reporting/math_pdf.py

# Existing reporting patterns
@src/reporting/single.py
@src/reporting/embed.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create audit data registry with 28 formula-to-code entries</name>
  <files>src/reporting/audit_registry.py</files>
  <action>
Create `src/reporting/audit_registry.py` containing a module-level list `AUDIT_ENTRIES: list[dict]` with exactly 28 entries — one per audited requirement ID. Do NOT reuse MATH_SECTIONS from math_pdf.py (per user decision — fresh curated registry).

Each entry dict must have these keys:
- `req_id`: str — requirement ID (e.g., "GRAPH-01")
- `category`: str — one of "Graph", "SVD", "AUROC", "Statistical", "Softmax", "Null Model"
- `title`: str — human-readable title
- `latex`: str — KaTeX-compatible LaTeX formula(s) for display mode. Use `\\[ ... \\]` delimiters for display math. Multiple formulas separated by line breaks.
- `code_location`: str — file:line reference (e.g., "src/graph/dcsbm.py:42"). Find the actual primary implementation line by reading the source files and audit test files referenced in each summary.
- `verdict`: str — one of "correct", "fixed", "concern"
- `fix_description`: str or None — description of fix if verdict is "fixed", None otherwise

Also create a helper `CATEGORIES: list[str]` listing category names in display order:
["Graph", "SVD", "AUROC", "Statistical", "Softmax", "Null Model"]

And a helper function `entries_by_category() -> dict[str, list[dict]]` that groups entries by category.

**Verdict assignments (from audit phase summaries):**

CORRECT (24 entries — no production code changes needed):
- GRAPH-01: DCSBM edge probability P_ij formula
- GRAPH-02: Walk sampling uniformity
- GRAPH-03: Block jumper designation
- GRAPH-05: Walk compliance rate
- SVD-01: QK^T attention matrix construction
- SVD-02: WvWo and AVWo matrix constructions
- SVD-03: All 5 singular-value-derived metrics
- SVD-04: Grassmannian distance (geodesic)
- SVD-06: Frenet-Serret curvature/torsion
- AUROC-01: AUROC rank-based computation
- AUROC-02: Lookback indexing (fence-post correct)
- AUROC-03: Predictive horizon consistency
- AUROC-04: Event extraction boundaries
- STAT-01: Shuffle permutation null
- STAT-02: Bootstrap BCa intervals
- STAT-03: Holm-Bonferroni correction
- STAT-04: Cohen's d effect size
- STAT-06: Exploratory/confirmatory split
- SFTX-01: LaTeX derivation correctness
- SFTX-02: Empirical bound verification
- SFTX-03: Bound assumptions
- NULL-01: Grassmannian drift code-path parity
- NULL-03: Column-filtered adjacency
- NULL-04: Holm-Bonferroni family separation

FIXED (4 entries — production code changed):
- GRAPH-04: Behavioral classification — fix: "Expanded 3-class RuleOutcome (NOT_APPLICABLE/FOLLOWED/VIOLATED) to 4-class enum (UNCONSTRAINED/PENDING/FOLLOWED/VIOLATED) with PENDING countdown labeling"
- SVD-05: Float16 spectrum storage — fix: "Upgraded spectrum trajectory storage from float16 to float32 in pipeline.py; float16 produced 1130% curvature error and 702M% torsion error"
- STAT-05: Spearman correlation — fix: "Changed measurement mode from Pearson (np.corrcoef) to Spearman (scipy.stats.spearmanr) per requirement specification"
- NULL-02: Marchenko-Pastur sigma^2 — fix: "Corrected sigma^2 calibration formula: E[lambda_MP(gamma, sigma^2)] = sigma^2, not sigma^2*(1+gamma)"

**LaTeX formulas:** Write fresh KaTeX-compatible LaTeX for each entry. Use display math (`\\[...\\]`). Reference the formulas in MATH_SECTIONS as content guidance but rewrite for KaTeX compatibility (no LaTeX-specific packages, use standard math notation). Keep formulas concise — the key formula per requirement, not full derivation blocks.

**Code locations:** Read the actual source files to find the correct primary implementation line number for each requirement. The code_location should point to the function/class definition or the specific formula line. Use the audit test files and summaries as guides for where to look.

Include a module docstring explaining this is the data source for the v1.2 mathematical audit report.
  </action>
  <verify>
    <automated>python -c "from src.reporting.audit_registry import AUDIT_ENTRIES, CATEGORIES, entries_by_category; assert len(AUDIT_ENTRIES) == 28, f'Expected 28, got {len(AUDIT_ENTRIES)}'; assert len(CATEGORIES) == 6; by_cat = entries_by_category(); assert set(by_cat.keys()) == set(CATEGORIES); assert sum(e['verdict'] == 'fixed' for e in AUDIT_ENTRIES) == 4; assert sum(e['verdict'] == 'correct' for e in AUDIT_ENTRIES) == 24; assert all(k in e for e in AUDIT_ENTRIES for k in ('req_id','category','title','latex','code_location','verdict','fix_description')); print('Registry validation passed: 28 entries, 6 categories, 24 correct, 4 fixed')"</automated>
  </verify>
  <done>audit_registry.py exists with 28 entries (24 correct, 4 fixed, 0 concern), all fields populated, entries_by_category helper works, LaTeX strings are KaTeX-compatible display math</done>
</task>

<task type="auto">
  <name>Task 2: Vendor KaTeX assets with base64-embedded fonts</name>
  <files>src/reporting/vendor/katex.min.js, src/reporting/vendor/katex.min.css</files>
  <action>
Download KaTeX v0.16.x (latest stable) JS and CSS files and place them in `src/reporting/vendor/`.

Steps:
1. Create `src/reporting/vendor/` directory
2. Download `katex.min.js` from KaTeX CDN (https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.js) using curl/wget
3. Download `katex.min.css` from KaTeX CDN (https://cdn.jsdelivr.net/npm/katex@0.16.11/dist/katex.min.css)
4. Download the KaTeX font files referenced in katex.min.css (the WOFF2 files from the fonts/ directory)
5. Convert each font file to a base64 data URI and replace the `url(fonts/...)` references in katex.min.css with `url(data:font/woff2;base64,...)` inline data URIs

This makes the CSS fully self-contained — no external font file references. The final HTML report can embed both JS and CSS inline to be completely self-contained.

If the CDN download fails (network restrictions), create a fallback: write a Python script `src/reporting/vendor/fetch_katex.py` that downloads and processes the files when run, with clear instructions. But attempt the direct download first.

Create `src/reporting/vendor/__init__.py` (empty) to make it a package, and add a `src/reporting/vendor/README.md` is NOT needed (per anti-documentation rule).

Verify the JS file contains the katex global and the CSS contains the base64 font data URIs.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; js = Path('src/reporting/vendor/katex.min.js'); css = Path('src/reporting/vendor/katex.min.css'); assert js.exists(), 'katex.min.js missing'; assert css.exists(), 'katex.min.css missing'; js_content = js.read_text(); css_content = css.read_text(); assert len(js_content) > 10000, f'JS too small: {len(js_content)}'; assert len(css_content) > 10000, f'CSS too small: {len(css_content)}'; assert 'katex' in js_content.lower(), 'JS does not contain katex'; print(f'KaTeX vendored: JS={len(js_content)} bytes, CSS={len(css_content)} bytes')"</automated>
  </verify>
  <done>KaTeX JS and CSS exist in src/reporting/vendor/, CSS has base64-embedded fonts (no external font file references), JS contains the katex rendering engine</done>
</task>

</tasks>

<verification>
1. Registry has exactly 28 entries with correct verdicts
2. All 6 categories represented
3. KaTeX files exist and are non-trivial size
4. All entries have all required fields
</verification>

<success_criteria>
- `from src.reporting.audit_registry import AUDIT_ENTRIES` succeeds and returns 28 entries
- KaTeX JS/CSS files exist in vendor/ directory
- 24 entries have verdict "correct", 4 have verdict "fixed"
- Every entry has latex, code_location, and title fields populated
</success_criteria>

<output>
After completion, create `.planning/phases/23-audit-report-generation/23-01-SUMMARY.md`
</output>
