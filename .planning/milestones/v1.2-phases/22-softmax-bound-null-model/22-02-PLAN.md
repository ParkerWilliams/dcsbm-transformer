---
phase: 22-softmax-bound-null-model
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/audit/test_null_model_parity.py
  - tests/audit/test_marchenko_pastur.py
autonomous: true
requirements: [NULL-01, NULL-02, NULL-03, NULL-04]

must_haves:
  truths:
    - "null_model.py imports fused_evaluate from the same module as run_experiment.py (code-path identity)"
    - "null_model.py imports holm_bonferroni and cohens_d from statistical_controls.py (verified in Phase 21)"
    - "Column-filtered adjacency produces zero entries to jumper vertices"
    - "Walks on filtered graph never visit jumper vertices (RuntimeError on violation)"
    - "Mann-Whitney U matches scipy.stats.mannwhitneyu on identical inputs"
    - "compare_null_vs_violation calls holm_bonferroni on its own p_array (separate family)"
    - "Position matching extracts drift at identical positions for both null and violation"
    - "Marchenko-Pastur PDF integrates to 1, CDF is monotone, KS test passes on known MP data"
    - "MP sigma^2 calibration E[lambda] = sigma^2*(1+gamma) is correctly implemented"
  artifacts:
    - path: "tests/audit/test_null_model_parity.py"
      provides: "Code-path parity, column-filtered adjacency, MW-U, Holm-Bonferroni family separation, position matching"
      min_lines: 250
    - path: "tests/audit/test_marchenko_pastur.py"
      provides: "MP PDF/CDF verification, sigma^2 calibration, KS test on synthetic MP data"
      min_lines: 100
  key_links:
    - from: "src/analysis/null_model.py"
      to: "src/evaluation/pipeline.py:fused_evaluate"
      via: "AST import verification"
      pattern: "from src.evaluation.pipeline import.*fused_evaluate"
    - from: "src/analysis/null_model.py"
      to: "src/analysis/statistical_controls.py:holm_bonferroni"
      via: "AST import verification"
      pattern: "from src.analysis.statistical_controls import.*holm_bonferroni"
    - from: "src/analysis/null_model.py:compare_null_vs_violation"
      to: "scipy.stats.mannwhitneyu"
      via: "MW-U test call"
      pattern: "mannwhitneyu"
---

<objective>
Audit the null model baseline methodology for code-path parity, statistical correctness, and family separation.

Purpose: Verify that null model Grassmannian drift uses identical SVD extraction as primary analysis (NULL-01), Mann-Whitney U is correctly applied (NULL-02), column-filtered adjacency removes all jumper paths (NULL-03), and null model uses a separate Holm-Bonferroni family (NULL-04). Also verify Marchenko-Pastur distribution formulas.

Output: Two audit test files verifying code-path parity via AST, column-filtered adjacency correctness, MW-U against scipy, Holm-Bonferroni family separation, position-matched drift extraction, and MP distribution formulas.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-softmax-bound-null-model/22-CONTEXT.md

Source files to audit:
@src/analysis/null_model.py

Prior work establishing patterns:
@.planning/phases/20-auroc-predictive-horizon/20-02-SUMMARY.md (AST-based import verification pattern)
@.planning/phases/21-statistical-controls/21-01-SUMMARY.md (holm_bonferroni and cohens_d verified correct)

<interfaces>
<!-- Key functions and contracts the executor needs -->

From src/analysis/null_model.py:
```python
def generate_null_walks(graph_data, jumpers, config, n_walks, seed=9999) -> np.ndarray:
    """Column-filtered adjacency approach. Raises RuntimeError if walks visit jumper vertices."""

def extract_position_matched_drift(metric_array, event_positions, max_lookback) -> dict[int, np.ndarray]:
    """For each j=1..max_lookback, extracts metric at (event_pos - j) across all sequences."""

def marchenko_pastur_pdf(x, gamma, sigma2=1.0) -> float:
    """MP PDF: sqrt((lam+ - x)(x - lam-)) / (2*pi*sigma^2*gamma*x) for x in [lam-, lam+]."""

def marchenko_pastur_cdf(x, gamma, sigma2=1.0):
    """MP CDF via numerical integration of PDF. Handles scalar and array inputs."""

def run_mp_ks_test(singular_values, gamma) -> dict:
    """KS test of squared SVs against MP. sigma^2 = mean(sv^2) / (1+gamma)."""

def compare_null_vs_violation(null_drift, violation_drift, alpha=0.05) -> dict:
    """MW-U + Cohen's d + Holm-Bonferroni at each lookback. SEPARATE family."""
```

From run_experiment.py (primary analysis imports):
```python
from src.analysis.auroc_horizon import run_auroc_analysis
from src.analysis.statistical_controls import apply_statistical_controls
```
Note: run_experiment.py does NOT import from null_model.py (separate pipeline).

AST audit pattern from Phase 20:
```python
import ast
tree = ast.parse(source_code)
for node in ast.walk(tree):
    if isinstance(node, ast.ImportFrom):
        # check module and names
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit null model code-path parity, column filtering, MW-U, and family separation (NULL-01, NULL-02, NULL-03, NULL-04)</name>
  <files>tests/audit/test_null_model_parity.py</files>
  <action>
Create tests/audit/test_null_model_parity.py following the Phase 20 AST-based audit pattern:

**Class TestCodePathParity (NULL-01):**
1. AST import check: parse src/analysis/null_model.py source code with ast.parse. Verify it imports `fused_evaluate` from `src.evaluation.pipeline`. This proves the null model uses the identical SVD extraction path as the primary analysis (run_experiment.py also imports from the same module, verified in Phase 20).
2. AST import check: verify null_model.py imports `extract_events` and `filter_contaminated_events` from `src.analysis.event_extraction` (same event extraction as primary path).
3. Verify run_null_evaluation calls fused_evaluate with the null walks but same graph_data, jumpers, config — read the source function body and verify via AST that fused_evaluate is called inside it. (Use ast.parse of just the function source or inspect module.)

**Class TestStatisticalImportParity (NULL-02, NULL-04):**
4. AST import check: verify null_model.py imports `holm_bonferroni` and `cohens_d` from `src.analysis.statistical_controls` (same functions verified correct in Phase 21, STAT-03 and STAT-04). Do NOT re-verify the math — just verify the import path.
5. AST import check: verify null_model.py imports `mannwhitneyu` from `scipy.stats`. Verify the call uses `alternative="two-sided"` and `method="auto"` by inspecting the source code string.

**Class TestColumnFilteredAdjacency (NULL-03):**
6. Build a small graph (n=10 vertices, known adjacency). Designate vertices {3, 7} as jumpers. Call generate_null_walks logic: build filtered adjacency by zeroing columns for jumper vertices. Verify:
   a. Filtered adjacency has zero in all entries of columns 3 and 7.
   b. Filtered adjacency preserves non-jumper connections.
7. Generate walks on the filtered graph (use generate_null_walks with the small graph). Verify no walk contains vertex 3 or vertex 7.
8. Per CONTEXT.md: inject a walk visiting a jumper vertex into the post-hoc verification step. Build walks where one contains jumper vertex 3. Verify RuntimeError is raised with the message mentioning jumper vertices.

**Class TestMannWhitneyUCorrectness (NULL-02):**
9. Verify MW-U matches scipy directly: construct two known distributions (null drift = [1.0, 1.5, 2.0, 2.5, 3.0], violation drift = [3.0, 3.5, 4.0, 4.5, 5.0]). Call scipy.stats.mannwhitneyu directly with alternative='two-sided'. Then call compare_null_vs_violation with these as single-lookback dicts ({1: np.array(null)}, {1: np.array(viol)}). Verify the U statistic and p_value_raw match scipy within 1e-10.
10. Verify MW-U with identical distributions returns high p-value (not significant).
11. Verify MW-U with clearly separated distributions returns low p-value (significant).

**Class TestHolmBonferroniSeparation (NULL-04):**
12. Per CONTEXT.md: verify compare_null_vs_violation calls holm_bonferroni on its OWN p_array (MW-U p-values from lookback distances), and this array never includes p-values from the primary AUROC analysis. Structural check:
    a. Read source code of compare_null_vs_violation.
    b. Verify via AST or string inspection that holm_bonferroni is called with `p_array` constructed from `raw_p_values` list, which is populated only from `mannwhitneyu` results within this function.
    c. Verify no external p-values are passed in (the function signature takes null_drift, violation_drift, alpha — no p_value parameter).
13. Functional test: call compare_null_vs_violation with 3 lookback distances, verify the returned p_value_adjusted values differ from the raw values (confirming Holm-Bonferroni was applied), and verify the adjustment is applied to the correct family size (3 tests, not combined with any external family).

**Class TestPositionMatchedDrift:**
14. Per CONTEXT.md: verify position matching. Build synthetic metric_array (shape [5, 20]) with known values. Set event_positions = [10, 15]. Set max_lookback = 3. Call extract_position_matched_drift. Verify:
    a. For j=1: values from columns 9 and 14 (positions 10-1 and 15-1) across all 5 sequences.
    b. For j=2: values from columns 8 and 13.
    c. For j=3: values from columns 7 and 12.
    d. NaN filtering works: insert NaN at metric_array[2, 9], verify it is excluded from j=1 result.
15. Verify both null and violation drift extraction use the SAME event_positions list and max_lookback. This is a structural check: read run_null_analysis source, verify violation_drift and null_drift are both computed with extract_position_matched_drift using the same event_positions and max_lookback variables.

Use established audit patterns: descriptive test classes, 1-2 line mathematical reasoning comments, torch.manual_seed(42) for reproducibility where needed.
  </action>
  <verify>
    <automated>cd /root/Repos/dcsbm-transformer && python -m pytest tests/audit/test_null_model_parity.py -x -v 2>&1 | tail -40</automated>
  </verify>
  <done>All tests pass. Code-path parity verified via AST (fused_evaluate, extract_events, holm_bonferroni, cohens_d all imported from correct modules). Column-filtered adjacency verified: zero entries for jumper columns, no walks visit jumpers, RuntimeError on violation. MW-U matches scipy within 1e-10. Holm-Bonferroni family separation confirmed: compare_null_vs_violation uses its own p_array with no external contamination. Position matching verified: both null and violation use same event_positions and max_lookback.</done>
</task>

<task type="auto">
  <name>Task 2: Audit Marchenko-Pastur distribution formulas and sigma^2 calibration (NULL-02)</name>
  <files>tests/audit/test_marchenko_pastur.py</files>
  <action>
Create tests/audit/test_marchenko_pastur.py to verify the MP distribution implementation:

**Class TestMarchenkoPasturPDF:**
1. Verify MP PDF formula: for known gamma=0.5, sigma2=1.0, compute lambda_minus = (1-sqrt(0.5))^2, lambda_plus = (1+sqrt(0.5))^2. Verify marchenko_pastur_pdf returns 0 outside [lambda_minus, lambda_plus] and positive inside.
2. Verify PDF integrates to 1.0: use scipy.integrate.quad to integrate marchenko_pastur_pdf from lambda_minus to lambda_plus. Verify result is within 1e-6 of 1.0. Test for gamma = 0.25, 0.5, 1.0, 2.0.
3. Verify PDF is non-negative everywhere in [lambda_minus, lambda_plus]: sample 100 points, verify all >= 0.

**Class TestMarchenkoPasturCDF:**
4. Verify CDF monotonicity: for gamma=0.5, compute CDF at 20 equally-spaced points in [lambda_minus, lambda_plus]. Verify CDF values are monotonically non-decreasing.
5. Verify CDF boundary values: CDF(lambda_minus) = 0 (within tolerance), CDF(lambda_plus) = 1.0 (within tolerance).
6. Verify CDF handles array inputs: pass a numpy array of 10 points, verify output is an array of same length.
7. Verify CDF handles scalar input: pass a single float, verify output is a float.

**Class TestSigma2Calibration:**
8. Per CONTEXT.md: verify sigma^2 calibration formula. Generate a known random matrix X (m x n) with iid N(0, sigma^2_true) entries. Compute eigenvalues of (1/n) X^T X. Compute sigma^2_estimated = mean(eigenvalues) / (1 + gamma) where gamma = m/n. Verify sigma^2_estimated is close to sigma^2_true (within 10% for m=200, n=400 which gives gamma=0.5). Use multiple seeds and verify mean error < 5%.
9. Verify the formula E[lambda] = sigma^2 * (1 + gamma): compute the theoretical mean eigenvalue sigma^2_true * (1 + gamma), compare to the actual mean eigenvalue from the random matrix. Should match within 5% for large enough m, n.

**Class TestMPKSTest:**
10. Per CONTEXT.md: build synthetic eigenvalues from a known MP distribution. Use gamma=0.5, sigma2=1.0. Sample from the MP distribution using inverse-CDF (or use rejection sampling):
    - Generate m=100, n=200 random matrix X ~ N(0, 1). Compute eigenvalues of (1/n) X^T @ X.
    - Call run_mp_ks_test with sqrt(eigenvalues) as "singular values" and gamma=0.5.
    - Verify KS p-value > 0.05 (cannot reject MP hypothesis).
11. Negative control: generate eigenvalues from a uniform distribution (NOT MP). Call run_mp_ks_test. Verify KS p-value < 0.05 (reject MP hypothesis, as data is not from MP distribution). Use enough samples (500+) for power.
12. Verify sigma^2 calibration in run_mp_ks_test: pass known singular values where sigma^2 can be computed analytically. Verify the returned sigma2 field matches expected value.

Use np.random.default_rng(42) for reproducibility. Mathematical reasoning comments on each assertion.
  </action>
  <verify>
    <automated>cd /root/Repos/dcsbm-transformer && python -m pytest tests/audit/test_marchenko_pastur.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>All tests pass. MP PDF verified: integrates to 1.0 for multiple gamma values, non-negative, zero outside support. CDF verified: monotone, correct boundary values, handles scalar and array inputs. Sigma^2 calibration verified: E[lambda] = sigma^2*(1+gamma) holds empirically for random matrices. KS test verified: passes for true MP data, rejects non-MP data.</done>
</task>

</tasks>

<verification>
Run all new audit tests alongside existing audit tests to verify no regressions:
```bash
cd /root/Repos/dcsbm-transformer && python -m pytest tests/audit/ -x -v 2>&1 | tail -50
```
All tests (existing + new) must pass.
</verification>

<success_criteria>
- tests/audit/test_null_model_parity.py exists with 15+ tests covering AST import parity, column filtering, MW-U, Holm-Bonferroni separation, position matching
- tests/audit/test_marchenko_pastur.py exists with 12+ tests covering PDF integration, CDF monotonicity, sigma^2 calibration, KS test positive and negative controls
- All tests pass with no regressions to existing test suite
- Code-path parity proven at AST level (fused_evaluate, extract_events, holm_bonferroni, cohens_d)
- Column-filtered adjacency proven to produce zero contamination
- MW-U matches scipy within 1e-10
- Holm-Bonferroni family separation structurally verified
- MP distribution formulas verified against random matrix theory
</success_criteria>

<output>
After completion, create `.planning/phases/22-softmax-bound-null-model/22-02-SUMMARY.md`
</output>
